#ifndef ADVANCED_BINARY_H
#define ADVANCED_BINARY_H

#include <stdio.h>
#include <stdlib.h>

int advanced_binary(int *array, size_t size, int value);

int binary_search_recursion(int *array, size_t low, size_t high, int value);

#endif
